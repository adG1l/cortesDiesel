[project]
name = "cortesdiesel"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "AdrianGil", email = "ruben.gil@jalisco.gob.mx" }
]
requires-python = ">=3.14"
dependencies = [
    "pyodbc>=5.3.0",
]

[project.scripts]
cortesdiesel = "cortesdiesel:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
